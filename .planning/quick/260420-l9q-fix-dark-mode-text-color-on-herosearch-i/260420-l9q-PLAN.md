---
phase: quick
plan: 260420-l9q
type: execute
wave: 1
depends_on: []
files_modified:
  - client/src/components/search/HeroSearch.tsx
autonomous: true
requirements: []
must_haves:
  truths:
    - "Typed text in HeroSearch input is visible in dark mode"
  artifacts:
    - path: "client/src/components/search/HeroSearch.tsx"
      provides: "Dark mode text color on search input"
      contains: "dark:text-gray-100"
  key_links: []
---

<objective>
Fix dark mode text visibility on the HeroSearch input field.

Purpose: The input at client/src/components/search/HeroSearch.tsx has dark:bg-gray-800 but no dark:text-* class, so typed text is invisible against the dark background.
Output: Input text is light-colored and readable in dark mode.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@client/src/components/search/HeroSearch.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add dark mode text color to HeroSearch input</name>
  <files>client/src/components/search/HeroSearch.tsx</files>
  <action>
On line 45 of HeroSearch.tsx, the input element's className string contains:
```
"flex-1 px-3 py-3 rounded-xl border border-gray-300 dark:border-gray-600 bg-white dark:bg-gray-800 text-lg text-start focus:outline-none focus:ring-2 focus:ring-primary focus:border-transparent"
```

Add `dark:text-gray-100` to this className, placing it after `dark:bg-gray-800`. Also add `dark:placeholder-gray-400` so the placeholder text ("הזינו מספר שחקן") remains visible too.

The resulting className should be:
```
"flex-1 px-3 py-3 rounded-xl border border-gray-300 dark:border-gray-600 bg-white dark:bg-gray-800 dark:text-gray-100 dark:placeholder-gray-400 text-lg text-start focus:outline-none focus:ring-2 focus:ring-primary focus:border-transparent"
```

No other changes to the file.
  </action>
  <verify>
    <automated>cd /home/rfreiman/code/chess-il && grep -q "dark:text-gray-100" client/src/components/search/HeroSearch.tsx && grep -q "dark:placeholder-gray-400" client/src/components/search/HeroSearch.tsx && echo "PASS" || echo "FAIL"</automated>
  </verify>
  <done>HeroSearch input has dark:text-gray-100 and dark:placeholder-gray-400 classes, making typed text and placeholder visible in dark mode.</done>
</task>

</tasks>

<verification>
- grep confirms dark:text-gray-100 present in HeroSearch.tsx
- grep confirms dark:placeholder-gray-400 present in HeroSearch.tsx
- No other lines in the file were modified
</verification>

<success_criteria>
Text typed into the HeroSearch input is visible (light gray) against the dark background in dark mode. Placeholder text is also visible.
</success_criteria>

<output>
After completion, create `.planning/quick/260420-l9q-fix-dark-mode-text-color-on-herosearch-i/260420-l9q-SUMMARY.md`
</output>
