---
phase: 2
slug: home-app-shell
status: draft
shadcn_initialized: false
preset: none
created: 2026-04-20
---

# Phase 2 — UI Design Contract

> Visual and interaction contract for the Home & App Shell phase. Generated by gsd-ui-researcher, verified by gsd-ui-checker.

---

## Design System

| Property | Value |
|----------|-------|
| Tool | none |
| Preset | not applicable |
| Component library | none (hand-rolled Tailwind components) |
| Icon library | lucide-react |
| Font | Heebo (self-hosted via @fontsource/heebo) |

**Source:** RESEARCH.md Standard Stack, CONTEXT.md D-13

---

## Spacing Scale

Declared values (all multiples of 4):

| Token | Value | Usage |
|-------|-------|-------|
| xs | 4px | Icon gaps, inline spacing between icon and label |
| sm | 8px | Compact element spacing, gap between nav items |
| base | 12px | Input padding, search row gap, dropdown item padding |
| md | 16px | Default element spacing, card internal padding, input padding-x |
| lg | 24px | Section padding, gap between hero search and saved players section |
| xl | 32px | Page horizontal padding on mobile (16px each side), card grid gap |
| 2xl | 48px | Hero search vertical centering margin-top |
| 3xl | 64px | Navbar offset (56px navbar height + 8px breathing room) |

Exceptions:
- Navbar height: 56px (h-14 = 3.5rem). Not a spacing token but a fixed dimension.
- Touch targets: minimum 44px for all interactive elements (buttons, icon buttons, card tap areas). Enforced via min-h-[44px] min-w-[44px] on interactive elements.

**Source:** Claude's Discretion (CONTEXT.md grants spacing decisions to Claude)

---

## Typography

| Role | Size | Weight | Line Height | Tailwind Class |
|------|------|--------|-------------|----------------|
| Body | 16px | 400 (regular) | 1.5 | text-base font-normal leading-normal |
| Label | 14px | 400 (regular) | 1.4 | text-sm font-normal leading-snug |
| Heading | 20px | 700 (bold) | 1.3 | text-xl font-bold leading-tight |
| Display | 28px | 700 (bold) | 1.2 | text-2xl font-bold leading-none sm:text-3xl |

**Font family:** `font-heebo` applied globally via `@layer base` on `html` element.

**Usage in this phase:**
- **Display:** App title in hero area (if present), not currently used in Phase 2
- **Heading:** "שחקנים שמורים" (Saved Players) section heading
- **Body:** Search input text, card content (player name, club), empty state body text
- **Label:** Card rating value, navbar items, empty state subtext, input placeholder

**Weight rationale:** Two weights only -- 400 (regular) and 700 (bold). Label is distinguished from Body by font size alone (14px vs 16px). Heading is distinguished from Display by font size alone (20px vs 28px). This keeps the typographic system lean and avoids loading unnecessary font weight files.

**Source:** Claude's Discretion (CONTEXT.md grants typography decisions to Claude). Heebo font from D-13.

---

## Color

| Role | Value | Usage |
|------|-------|-------|
| Dominant (60%) | #F9FAFB (gray-50) | Page background, main content area |
| Secondary (30%) | #FFFFFF (white) | Cards, navbar background, input backgrounds |
| Accent (10%) | #378ADD (primary blue) | See reserved list below |
| Positive | #639922 | Positive rating changes (Phase 3+, not used in Phase 2) |
| Negative | #E24B4A | Error states, negative rating changes (Phase 3+) |
| Pending | #EF9F27 | Pending badges (Phase 3+, not used in Phase 2) |

**Dark mode equivalents:**

| Role | Light | Dark |
|------|-------|------|
| Dominant (60%) | #F9FAFB (gray-50) | #111827 (gray-900) |
| Secondary (30%) | #FFFFFF (white) | #1F2937 (gray-800) |
| Text primary | #111827 (gray-900) | #F9FAFB (gray-50) |
| Text secondary | #6B7280 (gray-500) | #9CA3AF (gray-400) |
| Border | #E5E7EB (gray-200) | #374151 (gray-700) |
| Accent (10%) | #378ADD | #378ADD (unchanged) |

**Accent (#378ADD) reserved for:**
1. Search button ("חפש") background color
2. Search input focus ring (ring-2 ring-primary)
3. App logo/title text in navbar
4. Active navigation link indicator
5. Hover state on saved player cards (border highlight)

**Accent is NOT used for:** general text, backgrounds, borders at rest, disabled states, or decorative elements.

**Source:** CONTEXT.md D-15 (gray-50 background), D-14 (white cards), CLAUDE.md color palette (UI-04)

---

## Copywriting Contract

All copy is in Hebrew. Text direction is RTL.

| Element | Copy | Context |
|---------|------|---------|
| Primary CTA | חפש (Search) | Search button label, accompanies Search icon from lucide-react |
| Search placeholder | הזינו מספר שחקן (Enter player number) | Input placeholder text |
| Empty state heading | חפשו שחקן כדי להתחיל | Shown when no saved players exist |
| Empty state body | הזינו מספר שחקן בשורת החיפוש למעלה כדי לצפות בסטטיסטיקות | Guides user to the search input above |
| Saved players heading | שחקנים שמורים | Section heading above saved player card grid |
| Error: invalid input | (none - button disabled state) | Non-numeric or empty input disables the search button; no error message displayed |
| Error: navigation fail | שגיאה בטעינת הדף. נסו לרענן. (Error loading page. Try refreshing.) | Fallback error boundary message |
| Navbar: app name | &#9823; Chess IL | Navbar brand with pawn unicode character |
| Navbar: home aria-label | דף הבית (Home page) | aria-label on the Home link icon button in navbar |
| Navbar: compare tooltip | השוואת שחקנים (Player comparison) | Tooltip/aria-label on compare icon |
| Navbar: compare disabled | שמרו לפחות 2 שחקנים כדי להשוות (Save at least 2 players to compare) | Title attribute on disabled compare link |
| Navbar: theme toggle aria-label | החלפת מצב תצוגה (Toggle display mode) | aria-label on the dark mode toggle icon button |
| Recent searches heading | חיפושים אחרונים (Recent searches) | Dropdown heading above recent search suggestions |

**Destructive actions in this phase:** None. The remove button on saved player cards is Phase 4 scope.

**Source:** CONTEXT.md D-04 (CTA label), D-06 (empty state), RESEARCH.md Pattern 4 (placeholder). Error copy and section headings are Claude's Discretion.

---

## Component Inventory

Components this phase introduces, with their visual contract:

### Navbar
- Height: 56px (h-14)
- Position: fixed top, full width, z-50
- Background: white (dark: gray-800)
- Border: bottom 1px gray-200 (dark: gray-700)
- Shadow: shadow-sm
- Content max-width: 80rem (max-w-5xl), centered
- Internal padding: 16px horizontal
- Items: logo (start), icon group (end) with 8px gap between icons
- Icon button size: 40px (p-2 on 20px icons), meeting 44px touch target with gap spacing
- Home link: aria-label="דף הבית"
- Theme toggle: aria-label="החלפת מצב תצוגה"
- Compare link: aria-label="השוואת שחקנים"

### HeroSearch
- Container: centered, max-width 448px (max-w-md), full-width within container
- Vertical position: 48px from navbar offset (mt-12 from main top)
- Layout: flex row with 12px gap (gap-3), reversed in RTL (automatic via flex + dir)
- Input: flex-1, 12px padding-x (px-3), 12px padding-y (py-3), rounded-xl, border 1px gray-300 (dark: gray-600), text-lg (18px), focus ring 2px primary blue
- Button: 24px padding-x (px-6), 12px padding-y (py-3), rounded-xl, bg-primary, white text, font-normal (400), disabled: opacity-50 + cursor-not-allowed
- Input mode: numeric (triggers numeric keyboard on mobile)

### RecentSuggestions
- Position: absolute below search input, full width of search container
- Background: white (dark: gray-800), rounded-lg, shadow-md, border 1px gray-200
- Max items: 5 recent searches
- Each item: 12px padding (px-3 py-2), hover bg-gray-100 (dark: gray-700), cursor-pointer
- Item text: 14px, gray-700 (dark: gray-300)
- Heading: 14px, font-normal, gray-500, 8px padding-x, 4px padding-y

### PlayerCard
- Background: white (dark: gray-800)
- Border radius: 12px (rounded-xl per D-14)
- Shadow: shadow-sm (per D-14)
- Padding: 16px (p-4)
- Content: player name (16px, font-bold), rating (14px, font-normal, gray-600), club name (14px, font-normal, gray-500)
- Hover: subtle border color change to primary blue, transition-colors duration-150
- Clickable: entire card is a link to /player/:id
- Min-height: 80px for consistent grid appearance

### PlayerGrid
- Layout: CSS grid, 2 columns on mobile (grid-cols-2), 3 columns on desktop (md:grid-cols-3)
- Gap: 16px (gap-4)
- Container: max-width 80rem (max-w-5xl), centered, 16px horizontal padding

### EmptyState
- Centered layout: text-center, flex-col items-center
- Icon: Search icon from lucide-react, 48px (h-12 w-12), gray-400 color
- Heading: 20px (text-xl), font-bold, gray-700 (dark: gray-200), 16px margin-top from icon
- Body: 14px (text-sm), gray-500 (dark: gray-400), 8px margin-top from heading, max-width 320px

### ThemeToggle
- Button: 40px (p-2), rounded-lg, hover bg-gray-100 (dark: gray-700)
- Icon: Sun (light mode) / Moon (dark mode), 20px (h-5 w-5)
- Transition: icon swap only, no animation (keep it simple)
- Accessibility: aria-label="החלפת מצב תצוגה"

### AppLayout
- Root: min-h-screen, bg-gray-50 (dark: bg-gray-900)
- Main content: padding-top 64px (pt-16, offset for sticky 56px navbar + 8px)
- dir="rtl" and lang="he" on html element (set in index.html, not in React)

### Placeholder Pages
- PlayerPage: centered text "טוען..." (Loading...) -- replaced in Phase 3
- ComparePage: centered text "השוואת שחקנים" (Player comparison) -- replaced in Phase 5

---

## Interaction States

| Component | State | Visual Treatment |
|-----------|-------|------------------|
| Search button | Disabled (invalid input) | opacity-50, cursor-not-allowed, no hover effect |
| Search button | Enabled (valid input) | Full opacity, hover: bg-blue-600, cursor-pointer |
| Search button | Active (click) | Default browser active state |
| Search input | Default | border gray-300, bg white |
| Search input | Focus | ring-2 ring-primary (blue), border transparent |
| Compare nav link | Disabled (< 2 saved) | opacity-40, cursor-not-allowed, pointer-events-none |
| Compare nav link | Enabled (>= 2 saved) | Full opacity, hover bg-gray-100 |
| Player card | Default | shadow-sm, white bg |
| Player card | Hover | border-primary (blue border), transition-colors |
| Recent suggestion item | Default | transparent bg |
| Recent suggestion item | Hover | bg-gray-100 (dark: bg-gray-700) |
| Dark mode toggle | Light mode active | Sun icon visible |
| Dark mode toggle | Dark mode active | Moon icon visible |
| Nav icon button | Default | transparent bg |
| Nav icon button | Hover | bg-gray-100 (dark: bg-gray-700), rounded-lg |

---

## Responsive Breakpoints

| Breakpoint | Width | Layout Changes |
|------------|-------|----------------|
| Default (mobile) | 0-639px | 2-column card grid, full-width search, compact padding (16px) |
| sm | 640px+ | No changes for this phase |
| md | 768px+ | 3-column card grid, search max-width 448px centered |
| lg | 1024px+ | No additional changes |

**Mobile-first approach:** All base styles target 375px minimum width. Breakpoint classes add desktop enhancements.

**Source:** CONTEXT.md D-05 (2 cols mobile, 3 cols desktop), UI-02 (375px requirement)

---

## RTL Contract

| Rule | Implementation |
|------|----------------|
| Document direction | `dir="rtl" lang="he"` on `<html>` in index.html |
| Margin/padding | Use logical properties only: `ms-`, `me-`, `ps-`, `pe-` |
| Text alignment | Use `text-start`, `text-end` (never `text-left`, `text-right`) |
| Border radius | Use `rounded-s-`, `rounded-e-` for directional rounding |
| Positioning | Use `start-`, `end-` (never `left-`, `right-`) |
| Flexbox | flex + dir="rtl" auto-reverses row direction; no manual overrides needed |
| Banned classes | `ml-`, `mr-`, `pl-`, `pr-`, `left-`, `right-`, `text-left`, `text-right` |

**Source:** RESEARCH.md Pattern 3, Anti-Patterns section

---

## Registry Safety

| Registry | Blocks Used | Safety Gate |
|----------|-------------|-------------|
| shadcn official | none | not applicable -- shadcn not initialized |

No third-party registries. No shadcn dependency. All components are hand-rolled Tailwind.

**Source:** Design system detection found no `components.json`. Project uses plain Tailwind CSS per CLAUDE.md tech stack.

---

## Checker Sign-Off

- [ ] Dimension 1 Copywriting: PASS
- [ ] Dimension 2 Visuals: PASS
- [ ] Dimension 3 Color: PASS
- [ ] Dimension 4 Typography: PASS
- [ ] Dimension 5 Spacing: PASS
- [ ] Dimension 6 Registry Safety: PASS

**Approval:** pending
