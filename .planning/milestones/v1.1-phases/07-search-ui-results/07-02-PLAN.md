---
phase: 07-search-ui-results
plan: 02
type: execute
wave: 2
depends_on: ["07-01"]
files_modified:
  - client/src/components/clubs/ClubResultsTable.tsx
  - client/src/components/clubs/ClubResultsCards.tsx
  - client/src/components/clubs/ClubResultsEmpty.tsx
  - client/src/components/clubs/ClubFloatingBar.tsx
autonomous: true
requirements: [CRES-01, CRES-02, CRES-03]

must_haves:
  truths:
    - "Desktop users see search results in a table with columns: checkbox, name, ID, rating, club, age"
    - "Mobile users see search results as compact cards with checkbox, name, rating, age, club"
    - "User can select individual players via row/card checkboxes"
    - "Select-all checkbox in table header toggles all results and shows indeterminate when partially selected"
    - "Selected rows have bg-blue-50 / dark:bg-blue-900/20 highlight"
    - "Floating action bar appears when 1+ players selected showing count"
    - "Empty state shows Hebrew message when search returns 0 results"
    - "Player name is a clickable link to /player/:id"
  artifacts:
    - path: "client/src/components/clubs/ClubResultsTable.tsx"
      provides: "Desktop results table with checkboxes and selection"
      exports: ["ClubResultsTable"]
    - path: "client/src/components/clubs/ClubResultsCards.tsx"
      provides: "Mobile results card list with checkboxes"
      exports: ["ClubResultsCards"]
    - path: "client/src/components/clubs/ClubResultsEmpty.tsx"
      provides: "Empty and initial state for club search results"
      exports: ["ClubResultsEmpty", "ClubResultsInitial"]
    - path: "client/src/components/clubs/ClubFloatingBar.tsx"
      provides: "Floating action bar showing selection count"
      exports: ["ClubFloatingBar"]
  key_links:
    - from: "client/src/components/clubs/ClubResultsTable.tsx"
      to: "/player/:id"
      via: "Link component on player name"
      pattern: "<Link.*to=.*player/"
    - from: "client/src/components/clubs/ClubResultsTable.tsx"
      to: "Set<number>"
      via: "selected prop for checkbox state"
      pattern: "selected.*Set.*number"
    - from: "client/src/components/clubs/ClubFloatingBar.tsx"
      to: "selection count"
      via: "count prop drives visibility and text"
      pattern: "נבחרו.*שחקנים"
---

<objective>
Build the results display components: desktop table, mobile cards, selection checkboxes with select-all/indeterminate, floating action bar, and empty/initial states. These components receive data and selection state as props and are wired into the page in Plan 03.

Purpose: Create the complete results viewing and selection experience, responsive across desktop and mobile.
Output: ClubResultsTable, ClubResultsCards, ClubResultsEmpty, ClubFloatingBar components.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07-search-ui-results/07-CONTEXT.md
@.planning/phases/07-search-ui-results/07-RESEARCH.md
@.planning/phases/07-search-ui-results/07-UI-SPEC.md
@.planning/phases/07-search-ui-results/07-01-SUMMARY.md

<interfaces>
<!-- Key types from shared and patterns from existing components -->

From packages/shared/types.ts:
```typescript
export interface ClubSearchResult {
  id: number;              // Player ID
  name: string;            // Player name
  rating: number | null;   // Israeli rating, null if empty/unparseable
  club: string;            // Club name
  birthYear: number | null; // Birth year, null if empty/unparseable
}
```

From client/src/components/players/PlayerCard.tsx (card styling pattern):
```typescript
// Card container: "relative block p-4 bg-white dark:bg-gray-800 rounded-xl shadow-sm border transition-colors duration-150"
// Selected: "border-[#378ADD] ring-1 ring-[#378ADD]"
// Unselected: "border-transparent hover:border-primary"
// Name: "text-base font-bold text-gray-900 dark:text-gray-50"
// Details: "text-sm text-gray-600 dark:text-gray-400"
```

From client/src/components/players/EmptyState.tsx (empty state pattern):
```typescript
// Container: "text-center flex flex-col items-center py-12"
// Icon: "h-12 w-12 text-gray-400 dark:text-gray-500"
// Heading: "text-xl font-bold text-gray-700 dark:text-gray-200 mt-4"
// Body: "text-sm text-gray-500 dark:text-gray-400 mt-2 max-w-xs"
```

From client/src/pages/HomePage.tsx (floating button pattern):
```typescript
// Floating compare button:
// "fixed bottom-6 left-1/2 -translate-x-1/2 z-50 bg-[#378ADD] text-white rounded-full
//   px-8 py-3 shadow-lg hover:bg-blue-600 transition-all duration-300 font-medium text-lg
//   animate-[bounce_0.5s_ease-in-out_1]"
```

Selection state contract (will be managed by parent in Plan 03):
```typescript
// Props pattern for selection:
interface SelectionProps {
  results: ClubSearchResult[];
  selected: Set<number>;
  onToggle: (id: number) => void;
  onToggleAll: () => void;
}
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create ClubResultsTable and ClubResultsCards with checkbox selection</name>
  <files>client/src/components/clubs/ClubResultsTable.tsx, client/src/components/clubs/ClubResultsCards.tsx</files>
  <read_first>
    - packages/shared/types.ts (ClubSearchResult type — birthYear is number | null, rating is number | null)
    - client/src/components/players/PlayerCard.tsx (card styling pattern, selected state styling)
    - .planning/phases/07-search-ui-results/07-UI-SPEC.md (table columns, card layout, select-all checkbox, row checkbox specs)
    - .planning/phases/07-search-ui-results/07-RESEARCH.md (Pattern 3: indeterminate checkbox, Pattern 5: responsive table-to-cards, Pitfall 5: age from null birthYear)
  </read_first>
  <action>
    **ClubResultsTable.tsx (desktop >=640px):**
    - Export `function ClubResultsTable({ results, selected, onToggle, onToggleAll }: ClubResultsTableProps)`
    - Props: `results: ClubSearchResult[]`, `selected: Set<number>`, `onToggle: (id: number) => void`, `onToggleAll: () => void`
    - Container: `hidden sm:block` (only shows on desktop >=640px)
    - Table wrapper: `w-full bg-white dark:bg-gray-800 rounded-xl shadow-sm border border-gray-200 dark:border-gray-700 overflow-hidden`
    - Use `<table className="w-full">`, `<thead>`, `<tbody>` semantic HTML
    - Header row: `bg-gray-50 dark:bg-gray-700/50`
    - Header cells: `text-xs font-bold text-gray-500 dark:text-gray-400 px-4 py-3 text-start`

    **Select-all checkbox (in table header first column):**
    - Use `useRef<HTMLInputElement>` and `useEffect` to set `ref.current.indeterminate = selected.size > 0 && selected.size < results.length`
    - `checked={selected.size === results.length && results.length > 0}`
    - `onChange={onToggleAll}`
    - `aria-label="בחר הכל"`
    - Checkbox styling: `w-4 h-4 rounded accent-[#378ADD]`

    **Table columns (RTL order, per UI-SPEC):**
    1. Checkbox — 40px width (`w-10`)
    2. `שם` — player name as `<Link to={/player/${player.id}}>` with class `text-sm font-bold text-primary hover:underline`
    3. `מס' שחקן` — player.id, `w-20`
    4. `דירוג` — `player.rating ?? '—'`, `w-16`
    5. `מועדון` — player.club
    6. `גיל` — calculated from birthYear: `calculateAge(player.birthYear) ?? '—'`

    **calculateAge helper** (define in this file or as a shared util):
    ```typescript
    function calculateAge(birthYear: number | null): number | null {
      if (birthYear === null) return null;
      return new Date().getFullYear() - birthYear;
    }
    ```

    **Body cells:** `text-sm text-gray-700 dark:text-gray-300 px-4 py-3 border-t border-gray-100 dark:border-gray-700`
    **Row checkbox:** `w-4 h-4 rounded accent-[#378ADD]`, `checked={selected.has(player.id)}`, `onChange={() => onToggle(player.id)}`, `aria-label={"בחר " + player.name}`
    **Selected row:** `bg-blue-50 dark:bg-blue-900/20` (per D-13)
    **Hover row:** `hover:bg-gray-50 dark:hover:bg-gray-800/50` (only when not selected)

    Import `Link` from `react-router-dom` for player name links (per D-09: opens in same tab).

    **ClubResultsCards.tsx (mobile <640px):**
    - Export `function ClubResultsCards({ results, selected, onToggle, onToggleAll }: ClubResultsCardsProps)`
    - Same props as ClubResultsTable
    - Container: `sm:hidden` (only shows on mobile <640px)

    **Select-all row (above cards):**
    - A standalone div: `flex items-center gap-2 py-2 px-1 mb-3`
    - Checkbox with same indeterminate logic as table (useRef + useEffect)
    - Label text: `"בחר הכל (${results.length})"`, class `text-sm text-gray-600 dark:text-gray-400`

    **Player cards:**
    - Wrapper: `space-y-3`
    - Each card: `<div>` with `p-4 bg-white dark:bg-gray-800 rounded-xl shadow-sm border transition-colors duration-150`
    - Selected card: `border-[#378ADD] ring-1 ring-[#378ADD] bg-blue-50 dark:bg-blue-900/20`
    - Unselected card: `border-transparent`
    - Layout inside card: `flex items-start gap-3`
    - Checkbox at start: `w-4 h-4 mt-1 rounded accent-[#378ADD]`, `checked={selected.has(player.id)}`, `onChange={() => onToggle(player.id)}`
    - Content block: flex-1
      - Name: `<Link to={/player/${player.id}}>` with class `text-base font-bold text-primary hover:underline`
      - Details row: `text-sm text-gray-600 dark:text-gray-400` showing `"דירוג: ${player.rating ?? '—'} | גיל: ${calculateAge(player.birthYear) ?? '—'}"`
      - Club: `text-sm text-gray-500 dark:text-gray-400` showing `player.club`
    - Use same `calculateAge` helper as table component.
  </action>
  <verify>
    <automated>cd /home/rfreiman/code/chess-il && npx tsc --noEmit --project client/tsconfig.json 2>&1 | head -20</automated>
  </verify>
  <acceptance_criteria>
    - client/src/components/clubs/ClubResultsTable.tsx contains `export function ClubResultsTable`
    - client/src/components/clubs/ClubResultsTable.tsx contains `hidden sm:block` or `hidden sm:table`
    - client/src/components/clubs/ClubResultsTable.tsx contains `<table`
    - client/src/components/clubs/ClubResultsTable.tsx contains `<thead` and `<tbody`
    - client/src/components/clubs/ClubResultsTable.tsx contains `useRef`
    - client/src/components/clubs/ClubResultsTable.tsx contains `indeterminate`
    - client/src/components/clubs/ClubResultsTable.tsx contains `בחר הכל`
    - client/src/components/clubs/ClubResultsTable.tsx contains `<Link` and `/player/`
    - client/src/components/clubs/ClubResultsTable.tsx contains `bg-blue-50 dark:bg-blue-900/20`
    - client/src/components/clubs/ClubResultsTable.tsx contains `שם` and `מס' שחקן` and `דירוג` and `מועדון` and `גיל`
    - client/src/components/clubs/ClubResultsTable.tsx contains `calculateAge`
    - client/src/components/clubs/ClubResultsCards.tsx contains `export function ClubResultsCards`
    - client/src/components/clubs/ClubResultsCards.tsx contains `sm:hidden`
    - client/src/components/clubs/ClubResultsCards.tsx contains `<Link` and `/player/`
    - client/src/components/clubs/ClubResultsCards.tsx contains `בחר הכל`
    - client/src/components/clubs/ClubResultsCards.tsx contains `indeterminate`
    - TypeScript compilation has no errors
  </acceptance_criteria>
  <done>Desktop table renders 6 columns with select-all/indeterminate checkbox, mobile cards render with checkbox + name/rating/age/club, selected items have blue highlight, player names link to /player/:id</done>
</task>

<task type="auto">
  <name>Task 2: Create ClubResultsEmpty states and ClubFloatingBar</name>
  <files>client/src/components/clubs/ClubResultsEmpty.tsx, client/src/components/clubs/ClubFloatingBar.tsx</files>
  <read_first>
    - client/src/components/players/EmptyState.tsx (empty state pattern: icon + heading + body centered layout)
    - client/src/pages/HomePage.tsx (floating compare button pattern: fixed bottom-6 left-1/2 z-50 styling)
    - .planning/phases/07-search-ui-results/07-UI-SPEC.md (Component 11: floating bar, Component 13: empty state, Component 14: error state, Component 15: initial state)
  </read_first>
  <action>
    **ClubResultsEmpty.tsx:**
    Export two components from this file:

    1. `ClubResultsInitial` — shown before first search (UI-SPEC Component 15):
    - Container: `text-center flex flex-col items-center py-12`
    - Icon: `Users` from lucide-react, `h-12 w-12 text-gray-400 dark:text-gray-500`
    - Heading: `"חיפוש שחקני מועדון"`, class `text-xl font-bold text-gray-700 dark:text-gray-200 mt-4`
    - Body: `"בחרו מועדון ולחצו חיפוש כדי לראות את רשימת השחקנים"`, class `text-sm text-gray-500 dark:text-gray-400 mt-2 max-w-xs`

    2. `ClubResultsEmpty` — shown when search returns 0 results (UI-SPEC Component 13):
    - Container: `text-center flex flex-col items-center py-12`
    - Icon: `SearchX` from lucide-react, `h-12 w-12 text-gray-400 dark:text-gray-500`
    - Heading: `"לא נמצאו שחקנים"`, class `text-xl font-bold text-gray-700 dark:text-gray-200 mt-4`
    - Body: `"נסו לשנות את המועדון או להגדיל את טווח הגילאים"`, class `text-sm text-gray-500 dark:text-gray-400 mt-2 max-w-xs`

    **ClubFloatingBar.tsx (UI-SPEC Component 11):**
    - Export `function ClubFloatingBar({ count }: { count: number })`
    - If `count === 0`, return null (component disappears)
    - Position: `fixed bottom-6 left-1/2 -translate-x-1/2 z-50` (matches existing compare button position)
    - Style: `bg-[#378ADD] text-white rounded-full px-8 py-3 shadow-lg`
    - Animation: `animate-[bounce_0.5s_ease-in-out_1]` on appearance
    - Layout: `flex items-center gap-4`
    - Count text: `<span className="text-base font-bold">נבחרו ${count} שחקנים</span>`
    - Export placeholder button: `<button disabled className="flex items-center gap-1 opacity-50 cursor-not-allowed text-sm"><Download className="h-4 w-4" />ייצוא CSV</button>`
    - Import `Download` from lucide-react
    - Add `role="status"` and `aria-live="polite"` on the outer container for accessibility
    - Use a `key={count}` trick on the container or a re-mounting wrapper to re-trigger the bounce animation when count changes significantly (or keep it simple: only animate on first appear, not on count changes)
  </action>
  <verify>
    <automated>cd /home/rfreiman/code/chess-il && npx tsc --noEmit --project client/tsconfig.json 2>&1 | head -20</automated>
  </verify>
  <acceptance_criteria>
    - client/src/components/clubs/ClubResultsEmpty.tsx contains `export function ClubResultsInitial`
    - client/src/components/clubs/ClubResultsEmpty.tsx contains `export function ClubResultsEmpty`
    - client/src/components/clubs/ClubResultsEmpty.tsx contains `חיפוש שחקני מועדון`
    - client/src/components/clubs/ClubResultsEmpty.tsx contains `לא נמצאו שחקנים`
    - client/src/components/clubs/ClubResultsEmpty.tsx contains `נסו לשנות את המועדון`
    - client/src/components/clubs/ClubResultsEmpty.tsx contains `בחרו מועדון ולחצו חיפוש`
    - client/src/components/clubs/ClubFloatingBar.tsx contains `export function ClubFloatingBar`
    - client/src/components/clubs/ClubFloatingBar.tsx contains `נבחרו`
    - client/src/components/clubs/ClubFloatingBar.tsx contains `שחקנים`
    - client/src/components/clubs/ClubFloatingBar.tsx contains `ייצוא CSV`
    - client/src/components/clubs/ClubFloatingBar.tsx contains `disabled`
    - client/src/components/clubs/ClubFloatingBar.tsx contains `role="status"`
    - client/src/components/clubs/ClubFloatingBar.tsx contains `aria-live="polite"`
    - client/src/components/clubs/ClubFloatingBar.tsx contains `fixed bottom-6`
    - TypeScript compilation has no errors
  </acceptance_criteria>
  <done>ClubResultsEmpty shows search-not-found state, ClubResultsInitial shows pre-search state, ClubFloatingBar shows selection count with disabled export placeholder</done>
</task>

</tasks>

<verification>
- TypeScript compiles without errors: `cd /home/rfreiman/code/chess-il && npx tsc --noEmit --project client/tsconfig.json`
- All four components export expected functions
- ClubResultsTable has indeterminate checkbox logic via useRef
- ClubResultsCards has select-all with indeterminate
- Player names are Link elements to /player/:id
- ClubFloatingBar has role="status" and aria-live="polite"
- Hebrew copy matches UI-SPEC copywriting contract exactly
</verification>

<success_criteria>
- Desktop table renders with 6 columns, select-all checkbox with indeterminate state
- Mobile cards render with checkbox, name link, rating, age, club
- Selected items highlighted with bg-blue-50 / dark:bg-blue-900/20
- Floating bar appears when count > 0 with disabled export button
- Empty and initial states show appropriate Hebrew messages
- All components compile with TypeScript
</success_criteria>

<output>
After completion, create `.planning/phases/07-search-ui-results/07-02-SUMMARY.md`
</output>
